import { join } from 'path'

export const TEXT_FIXTURES_DIR = join(__dirname, '..', '..', 'testFixtures')
export const FAKE_ESM_DIR = join(TEXT_FIXTURES_DIR, 'fakeEsm')
export const TEMP_DIR = join(TEXT_FIXTURES_DIR, '..', 'tmp')
export const FAKE_SWAP_DIR = join(TEXT_FIXTURES_DIR, 'fakeSwapFolder')
